{% extends '::Backend/base.html.twig' %}

{% block body -%}
<aside class="right-side">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>Anexos</h1>
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="row ">
            <div class="col-xs-12 bs-50">
                <a href="{{ path('backend_lesson', { 'course': lesson.course.id }) }}" class="btn btn-default">Voltar</a>
                <a href="{{ path('backend_lessonfiles_new',{'lesson': lesson.id}) }}" class="btn btn-success">Novo</a>
            </div>
        </div>
        <br>

        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">Listagem de Anexos</h3>
                    </div><!-- /.box-header -->
                    <div class="box-body table-responsive no-padding">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th></th>
                                    <th>Nome</th>
                                    <th>Ações</th>
                                </tr>
                            </thead>
                            <tbody class="sortable">
                            {% for entity in lessonFiles %}
                                <tr id="lessonfiles_order_{{entity.id}}">
                                    <td class="sortable-lesson"><i class="fa fa-bars" aria-hidden="true"></i></td>
                                    <td>{{ entity.name }}</td>
                                    <td>
                                        <a href="{{ path('backend_lessonfiles_edit', { 'id': entity.id }) }}" class="btn btn-primary">Editar</a>
                                        <a href="javascript:void(0);" data-url="{{ path('backend_lessonfiles_delete', { 'id': entity.id }) }}" class="btn btn-danger delete_item">Excluir</a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div><!-- /.box-body -->
                </div><!-- /.box -->
            </div>
        </div>
    </section><!-- /.content -->
</aside><!-- /.right-side -->
{% endblock %}
